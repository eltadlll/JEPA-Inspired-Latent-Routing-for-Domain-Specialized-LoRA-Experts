# Copy to .env and fill in as needed. Loaded via `python-dotenv` if you wire
# it up in main.py, or export these in your shell before running the CLI.

# Only required if you hit GitHub API rate limits while cloning many repos.
GITHUB_TOKEN=

# Only required if any configured Hugging Face dataset is gated/private.
HUGGINGFACE_TOKEN=
