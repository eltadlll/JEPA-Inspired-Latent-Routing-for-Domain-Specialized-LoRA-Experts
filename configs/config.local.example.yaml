# Example override file. Copy to config.local.yaml and adjust for local runs
# (e.g. smaller source lists for a fast smoke test). Pass with:
#   python main.py run --config configs/config.yaml --override configs/config.local.yaml

quality:
  min_quality_score: 0.4

instruction_generation:
  max_examples_per_document: 1

export:
  formats: ["jsonl"]
